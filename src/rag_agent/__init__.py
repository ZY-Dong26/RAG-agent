"""RAG Agent 核心包。

包内按职责分为：
    ingestion：把 PDF 转换为带来源和页码的统一文档记录；
    indexing：切块、向量化、按文档缓存以及 FAISS 索引发布；
    qa：检索、答案生成和供命令行/未来前端共用的问答服务；
    common：不包含业务判断的文件与持久化辅助函数。

这里不主动导入模型或向量库，避免仅读取配置时就加载大型依赖。
"""

