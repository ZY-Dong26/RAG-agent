# Copy to .env only if .env does not already exist. Never commit real API keys.
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# Relative paths are resolved against the project root; absolute paths also work.
EMBEDDING_MODEL=model/Qwen3-Embedding-0.6B
